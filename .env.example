# Copy to .env and fill in. Without Supabase values the app runs in DEMO mode
# (simulated data, no login).

# Google Maps (tactical map) — see GOOGLE_MAPS_SETUP.md
VITE_GOOGLE_MAPS_API_KEY=

# Supabase — Project Settings > API in your Supabase dashboard
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
